name = "cnk-mitatecho"
compatibility_date = "2025-01-25"
pages_build_output_dir = "out"

[placement]
mode = "smart"
hint = "wnam"

[[d1_databases]]
binding = "DB"
database_name = "prairie-cnk-db"
database_id = "dc61e8a3-e575-4abb-93d5-e0eca85ae039"

# NOTE: /api/submit のレート制限は D1 の rate_limits テーブルで実装
# （Pages Functions は Rate Limiting binding に未対応のため）
